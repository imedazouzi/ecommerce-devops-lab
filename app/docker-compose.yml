version: '3.8'

services:

  app:
    build: .
    ports:
      - "5001:5001"
    environment:
      - MONGO_URI=mongodb://mongo:27017/ecomstore
    depends_on:
      - mongo

  mongo:
    image: mongo:6
    ports:
      - "27017:27017"
